<bx:output>
<header class="mb-4">
    <p class="text-muted mb-1 text-uppercase small">Author</p>
    <h1 class="h3 mb-1">#encodeForHTML( prc.author.getDisplayName() ?: "Author" )#</h1>
    <bx:if len( prc.author.getBio() ?: "" )>
        <p class="text-muted mb-0">#encodeForHTML( prc.author.getBio() )#</p>
    </bx:if>
</header>

<bx:if ( prc.posts ?: [] ).len()>
    <div class="row g-4">
        <bx:loop array="#prc.posts#" item="post">
            <div class="col-md-6">
                #view( view = "bx-blogger-fallback/partials/post-card", args = { "post" : post } )#
            </div>
        </bx:loop>
    </div>
    <bx:if structKeyExists( prc, "pagination" )>
        <div class="mt-4">
            #view( view = "bx-blogger-fallback/partials/pagination" )#
        </div>
    </bx:if>
<bx:else>
    <p class="text-muted">No posts by this author yet.</p>
</bx:if>
</bx:output>

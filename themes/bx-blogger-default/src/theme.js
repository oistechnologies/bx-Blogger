/**
 * bx-Blogger — bx-blogger-default theme entry (Chunk 3.C)
 *
 * Vite's build pipeline walks `themes/{slug}/src/theme.js` as its entry
 * points. We import the SCSS here so Vite extracts it into a sibling
 * `theme.css` asset.
 *
 * This file is also the landing zone for theme-scoped JavaScript
 * (Phase 3.D adds the Prism.js autoloader + Bootstrap 5.3 data-bs-theme
 * dark-mode toggle hooks here).
 */
import "./theme.scss";
